var mocha = require('mocha');
var should = require('chai').should();
var expect = require('chai').expect;
var mongoose = require('mongoose');
var list = require('../../server/lists/listController.js');


// TODO: Write tests!

// getList should return a user's list
/**
 * addItem should create a new item if it doesn't exist
 * and add it to the user's list. If the item exists, 
 * it should add the reference to the item to the user's list.
 * It should return the item in the response body. 
 */ 

