var React = require('react');

var Register = React.createClass({
  render: function() {
    return (
      <div id="register">
        Register
      </div>
    );
  }
});

module.exports = Register;
